apiVersion: v1

kind: Pod

metadata:
  name: worker-app-prod
  labels:
      name: worker-app-prod
      app: demo-voting-app

spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker